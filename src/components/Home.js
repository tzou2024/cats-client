import CatsIndex from "./cats/CatsIndex"

const Home = (props) => {
	// const { msgAlert, user } = props
	// console.log('props in home', props)

	return (
		<>
			<CatsIndex />
		</>
	)
}

export default Home
